# ⚡ Быстрая настройка (5 минут)

## 1️⃣ Настройте .env

```bash
nano .env
```

**Обязательно измените:**
```env
# Ваш Telegram бот (без @)
TELEGRAM_BOT_USERNAME=MySalesBot  # ← ЗАМЕНИТЕ НА ИМЯ ВАШЕГО БОТА!

# Опционально для email (если используете)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

## 2️⃣ Запустите все

### Terminal 1: MongoDB
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### Terminal 2: Backend
```bash
npm run start:dev
```
✅ Backend: http://localhost:9022

### Terminal 3: Frontend
```bash
cd frontend
npm install  # только первый раз
npm run dev
```
✅ Frontend: http://localhost:3001

## 3️⃣ Откройте в браузере

```
http://localhost:3001
```

## 4️⃣ Протестируйте

### Telegram регистрация:
1. Выберите "📱 Telegram"
2. Введите имя: `Test User`
3. Введите телефон: `+79991234567`
4. Нажмите "Зарегистрироваться"
5. На странице верификации нажмите **"📱 Перейти в бота за кодом"**
6. Откроется Telegram бот
7. Код будет в логах backend (пока нет реального бота)
8. Введите код
9. Создайте бота! 🎉

### Email регистрация:
1. Выберите "✉️ Email"
2. Введите имя и email
3. Код придет на email (если настроен SMTP)
4. Введите код
5. Готово! 🎉

## 🔍 Где взять код (пока нет реального бота)?

Смотрите в логах backend в Terminal 2:

```
[VerificationService] Generated verification code: 123456
```

## 🎯 Что работает

- ✅ Регистрация через Email (name + email)
- ✅ Регистрация через Telegram (name + phone)
- ✅ **Кнопка перехода в бота** (для Telegram)
- ✅ Верификация кодом (без паролей)
- ✅ Настройка бота (TG/VK токен + промпты)
- ✅ Выбор тарифа (3 плана)
- ✅ Дашборд (статистика, управление)
- ✅ Возврат на любой шаг

## ⚠️ Важно!

### Для production
1. Измените `TELEGRAM_BOT_USERNAME` на реальное имя бота
2. Настройте SMTP для email
3. Настройте реального Telegram бота для отправки кодов
4. Добавьте реальную оплату

### Для разработки
- Коды пока только в логах backend
- SMTP можно не настраивать (коды в логах)
- Telegram бота можно не настраивать (коды в логах)

## 📖 Документация

- **FINAL_SUMMARY.md** - полное резюме всех изменений
- **TELEGRAM_BOT_BUTTON.md** - детали кнопки
- **frontend/README.md** - frontend документация
- **START_HERE.md** - подробная инструкция

## 🆘 Проблемы?

### npm install зависает (frontend)
```bash
cd frontend
rm -rf node_modules package-lock.json
yarn install  # используйте yarn вместо npm
```

### Backend не запускается
```bash
# Убейте процесс на порту
lsof -ti:9022 | xargs kill -9
# Запустите снова
npm run start:dev
```

### Код не приходит в Telegram
Это нормально! Пока нет настроенного бота.  
**Смотрите код в логах backend** в Terminal 2.

---

## 🎉 Готово к работе!

Откройте http://localhost:3001 и создайте первого бота! 🚀

**Время запуска:** 5 минут  
**Сложность:** Легко

**Happy coding!** 💙

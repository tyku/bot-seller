# Bot Seller Frontend

Современный фронтенд для платформы создания продающих ботов.

## Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **React Hook Form** - управление формами
- **Zod** - валидация
- **Axios** - HTTP клиент

## Особенности

### 🎯 Wizard Flow
Пошаговый процесс настройки бота:
1. **Регистрация** - создание аккаунта
2. **Верификация** - подтверждение через email или Telegram
3. **Настройка бота** - конфигурация бота и промптов
4. **Оплата** - выбор тарифа
5. **Дашборд** - управление ботом

### ✨ Возможности
- Возврат на любой шаг для редактирования
- Сохранение прогресса в localStorage
- Валидация форм в реальном времени
- Простой и понятный интерфейс
- Адаптивный дизайн

## Установка

```bash
cd frontend
npm install
```

## Запуск

### Development
```bash
npm run dev
```

Приложение будет доступно на http://localhost:3001

### Production
```bash
npm run build
npm run start
```

## Структура проекта

```
frontend/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Корневой layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── ui/               # UI компоненты
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   └── Card.tsx
│   ├── steps/            # Шаги wizard
│   │   ├── RegisterStep.tsx
│   │   ├── VerifyStep.tsx
│   │   ├── SettingsStep.tsx
│   │   ├── PaymentStep.tsx
│   │   └── DashboardStep.tsx
│   └── StepIndicator.tsx # Индикатор прогресса
├── contexts/             # React contexts
│   └── WizardContext.tsx # Контекст wizard
├── lib/                  # Утилиты
│   ├── api.ts           # API клиент
│   └── types.ts         # TypeScript типы
└── .env.local           # Переменные окружения
```

## Конфигурация

### Переменные окружения

Создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## API Integration

Фронтенд интегрируется с NestJS backend через REST API:

### Auth Endpoints
- `POST /auth/register` - регистрация
- `POST /auth/login` - вход
- `POST /auth/verify` - верификация кода
- `POST /auth/resend-code` - повторная отправка кода

### Customer Settings Endpoints
- `POST /customer-settings` - создание настроек
- `GET /customer-settings` - получение настроек
- `PATCH /customer-settings/:id` - обновление
- `DELETE /customer-settings/:id` - удаление

### Customer Endpoints
- `GET /customers/me` - получение текущего пользователя

## Wizard Context

Wizard Context управляет состоянием многошагового процесса:

```typescript
const { 
  currentStep,      // Текущий шаг
  completedSteps,   // Завершенные шаги
  user,             // Данные пользователя
  settings,         // Настройки бота
  setStep,          // Переход на шаг
  completeStep,     // Отметить шаг как завершенный
  canAccessStep     // Проверка доступа к шагу
} = useWizard();
```

## Компоненты

### RegisterStep
Форма регистрации с:
- Валидацией email, телефона, пароля
- Выбором метода верификации (email/telegram)
- Zod схемой валидации

### VerifyStep
Верификация кода с:
- Вводом кода
- Повторной отправкой
- Таймером (можно добавить)

### SettingsStep
Настройка бота:
- Выбор типа (Telegram/VK)
- Ввод токена
- Управление промптами (добавление/удаление)

### PaymentStep
Выбор тарифа:
- 3 тарифа: Starter, Pro, Business
- Сравнение функций
- Симуляция оплаты

### DashboardStep
Управление ботом:
- Статистика в реальном времени
- Настройки бота
- Быстрые действия
- История сообщений

## UI Components

### Button
```tsx
<Button 
  variant="primary" // primary | secondary | outline
  size="md"        // sm | md | lg
  isLoading={false}
>
  Click me
</Button>
```

### Input
```tsx
<Input 
  label="Email"
  error="Invalid email"
  helpText="Enter your email"
/>
```

### Card
```tsx
<Card className="custom-class">
  Content
</Card>
```

## Стилизация

Использует Tailwind CSS с кастомной конфигурацией:
- Градиентный фон
- Анимации
- Адаптивный дизайн
- Современная цветовая схема

## TODO

- [ ] Добавить реальную интеграцию оплаты (Stripe/Yookassa)
- [ ] Добавить полную аналитику в дашборд
- [ ] Добавить историю сообщений
- [ ] Добавить настройку уведомлений
- [ ] Добавить темную тему
- [ ] Добавить мультиязычность
- [ ] Добавить тесты (Jest + React Testing Library)

## Разработка

### Добавление нового шага

1. Создайте компонент в `components/steps/`
2. Добавьте тип шага в `lib/types.ts`
3. Добавьте шаг в `STEP_ORDER` в `WizardContext.tsx`
4. Добавьте в `StepIndicator.tsx`
5. Добавьте в `switch` в `app/page.tsx`

### Добавление нового API endpoint

Добавьте в `lib/api.ts`:

```typescript
export const yourApi = {
  method: async (data: any) => {
    const response = await api.post('/your-endpoint', data);
    return response.data;
  },
};
```

## License

UNLICENSED

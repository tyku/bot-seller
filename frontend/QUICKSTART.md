# 🚀 Быстрый старт Frontend

## Проблема с npm install

Если `npm install` зависает, попробуйте следующие решения:

### Решение 1: Использовать yarn

```bash
cd frontend

# Установить yarn (если нет)
npm install -g yarn

# Установить зависимости
yarn install

# Запустить dev сервер
yarn dev
```

### Решение 2: Очистить кэш npm

```bash
cd frontend

# Удалить package-lock.json
rm -f package-lock.json

# Очистить кэш
npm cache clean --force

# Попробовать снова
npm install

# Запустить
npm run dev
```

### Решение 3: Использовать другой registry

```bash
cd frontend

# Использовать китайское зеркало (быстрее)
npm install --registry=https://registry.npmmirror.com

# Или вернуться к стандартному
npm install --registry=https://registry.npmjs.org
```

### Решение 4: Установить зависимости по одной

```bash
cd frontend

# Базовые
npm install next react react-dom

# Dev зависимости  
npm install -D typescript @types/node @types/react @types/react-dom
npm install -D tailwindcss @tailwindcss/postcss
npm install -D eslint eslint-config-next

# Дополнительные
npm install react-hook-form @hookform/resolvers zod axios
```

## Запуск проекта

После успешной установки:

```bash
cd frontend
npm run dev
```

Откройте http://localhost:3000 в браузере.

## Backend

Не забудьте запустить backend:

```bash
# В корне проекта
npm run start:dev
```

Backend будет на http://localhost:3000

## Возможные порты

- Frontend: `http://localhost:3000` (или 3001 если 3000 занят)
- Backend: `http://localhost:3000`

Если порты конфликтуют, измените в `frontend/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000
```

И запустите frontend на другом порту:

```bash
npm run dev -- -p 3001
```

## Проверка работы

1. Откройте http://localhost:3001 (или 3000)
2. Вы должны увидеть страницу регистрации
3. Заполните форму регистрации
4. Следуйте шагам wizard

## Структура созданных файлов

```
frontend/
├── app/
│   ├── layout.tsx          ✅ Корневой layout
│   ├── page.tsx            ✅ Главная с wizard
│   └── globals.css         ✅ Стили
├── components/
│   ├── ui/
│   │   ├── Button.tsx      ✅ Компонент кнопки
│   │   ├── Input.tsx       ✅ Компонент input
│   │   └── Card.tsx        ✅ Компонент карточки
│   ├── steps/
│   │   ├── RegisterStep.tsx   ✅ Шаг регистрации
│   │   ├── VerifyStep.tsx     ✅ Шаг верификации
│   │   ├── SettingsStep.tsx   ✅ Настройка бота
│   │   ├── PaymentStep.tsx    ✅ Выбор тарифа
│   │   └── DashboardStep.tsx  ✅ Дашборд
│   └── StepIndicator.tsx      ✅ Индикатор шагов
├── contexts/
│   └── WizardContext.tsx   ✅ Контекст wizard
├── lib/
│   ├── api.ts             ✅ API клиент
│   └── types.ts           ✅ TypeScript типы
├── .env.local             ✅ Переменные окружения
├── package.json           ✅ Зависимости
├── README.md              ✅ Документация
└── QUICKSTART.md          ✅ Быстрый старт
```

## Возможные проблемы

### 1. "Module not found"

Убедитесь что все зависимости установлены:

```bash
npm install
```

### 2. "Port 3000 already in use"

```bash
# Найти процесс
lsof -i :3000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
npm run dev -- -p 3001
```

### 3. "Cannot connect to backend"

Проверьте что backend запущен:

```bash
cd ..  # в корень проекта
npm run start:dev
```

### 4. CORS ошибки

В backend (`src/main.ts`) должно быть:

```typescript
app.enableCors({
  origin: 'http://localhost:3001',  // или ваш порт
  credentials: true,
});
```

## Следующие шаги

1. ✅ Установить зависимости
2. ✅ Запустить backend
3. ✅ Запустить frontend  
4. 🎯 Протестировать wizard flow
5. 🎨 Кастомизировать дизайн (если нужно)
6. 🚀 Развернуть на production

## Дополнительно

- Backend API документация: `../src/customer/README.md`
- Backend API настройки: `../src/customer-settings/README.md`
- Frontend README: `./README.md`

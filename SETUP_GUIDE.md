# 🔧 Подробная инструкция по настройке

## 📧 Настройка Email (SMTP)

### Вариант 1: Gmail (рекомендуется для старта)

**Шаг 1: Создайте App Password**

1. Перейдите на https://myaccount.google.com/security
2. Включите двухфакторную аутентификацию (2FA)
3. Перейдите на https://myaccount.google.com/apppasswords
4. Выберите:
   - Приложение: "Почта"
   - Устройство: "Другое" → введите "Bot Seller API"
5. Нажмите "Создать"
6. Скопируйте сгенерированный пароль (16 символов)

**Шаг 2: Настройте .env**

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=abcdefghijklmnop  # App Password из шага 1
SMTP_FROM=your-email@gmail.com
```

**Ограничения:**
- До 500 писем в день
- Может попадать в спам у некоторых получателей
- Бесплатно

---

### Вариант 2: Yandex Mail

**Для обычной почты:**

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.ru
SMTP_PASS=your-password
SMTP_FROM=your-email@yandex.ru
```

**Для почты на своем домене:**

1. Зарегистрируйте домен в Yandex Connect: https://connect.yandex.ru
2. Настройте DNS записи
3. Создайте почтовый ящик noreply@yourdomain.com

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your-password
SMTP_FROM=noreply@yourdomain.com
```

---

### Вариант 3: Mailgun (профессиональный сервис)

**Шаг 1: Регистрация**
1. Зарегистрируйтесь на https://www.mailgun.com
2. Добавьте свой домен
3. Настройте DNS записи (SPF, DKIM, MX)

**Шаг 2: Получите SMTP credentials**
1. В панели Mailgun → Sending → Domain settings
2. Найдите раздел "SMTP credentials"
3. Создайте новые credentials

**Шаг 3: Настройте .env**

```env
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@mg.yourdomain.com
SMTP_PASS=your-mailgun-password
SMTP_FROM=noreply@yourdomain.com
```

**Преимущества:**
- Высокая доставляемость
- Аналитика и логи
- API для отслеживания
- Первые 5000 писем/месяц бесплатно (3 месяца)
- Далее $0.80 за 1000 писем

---

### Вариант 4: SendGrid

**Шаг 1: Регистрация**
1. Зарегистрируйтесь на https://sendgrid.com
2. Подтвердите email
3. Создайте API Key

**Шаг 2: Настройте .env**

```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.xxxxxxxxxxxxxxxxxxxxx  # Ваш API Key
SMTP_FROM=noreply@yourdomain.com
```

**Преимущества:**
- Бесплатно 100 писем/день
- От $15/месяц за 40,000 писем
- Отличная документация

---

### Вариант 5: Mailtrap (только для тестирования)

**Для разработки:**

```env
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-mailtrap-username
SMTP_PASS=your-mailtrap-password
SMTP_FROM=test@example.com
```

1. Зарегистрируйтесь на https://mailtrap.io
2. Создайте inbox
3. Скопируйте SMTP credentials

**Важно:** Письма не будут доходить до реальных пользователей! Все попадает в виртуальный inbox.

---

## 📱 Настройка Telegram

### Текущая реализация

Сейчас в проекте есть **простой модуль** (`TelegramService`), который:
- Хранит коды в памяти
- Предоставляет API для получения кодов
- НЕ отправляет сообщения напрямую

Это сделано специально, чтобы не мешать будущей реализации multi-tenant webhook сервера.

### Опция 1: Использовать как есть (без реальной отправки)

**Как работает:**

1. Пользователь регистрируется с `verificationMethod: "telegram"`
2. Код сохраняется в памяти
3. **Фронтенд** получает код через `GET /telegram/verification/:username`
4. **Фронтенд** показывает код пользователю
5. Пользователь вводит код в форме верификации

**Плюсы:** Работает без Telegram бота, просто и понятно  
**Минусы:** Код не приходит в Telegram автоматически

---

### Опция 2: Добавить реального Telegram бота

**Шаг 1: Создайте бота**

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте команду:
   ```
   /newbot
   ```
4. Выберите имя: `Bot Seller Verification Bot`
5. Выберите username: `@bot_seller_verification_bot`
6. Получите токен: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

**Шаг 2: Установите зависимость**

```bash
npm install telegraf
```

**Шаг 3: Добавьте токен в .env**

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Шаг 4: Создайте простого бота**

Я создал пример в файле `TELEGRAM_BOT_EXAMPLE.md`. Запустите бота отдельным процессом:

```bash
node verification-bot.js
```

**Как работает с ботом:**

```
User                    Frontend                API                   Bot
  |                        |                      |                     |
  |  Register              |                      |                     |
  |----------------------->|                      |                     |
  |                        |  POST /auth/register |                     |
  |                        |--------------------->|                     |
  |                        |                      | Store code          |
  |                        |                      |-------------------->|
  |                        |  Code stored         |                     |
  |                        |<---------------------|                     |
  |                        |                      |                     |
  |  "Open Telegram bot"   |                      |                     |
  |<-----------------------|                      |                     |
  |                        |                      |                     |
  |  /verify               |                      |                     |
  |------------------------------------------------>|  GET /telegram/verification
  |                        |                      |<--------------------|
  |  Your code: 123456     |                      |  Return code        |
  |<------------------------------------------------|-------------------->|
  |                        |                      |                     |
  |  /link 123456          |                      |                     |
  |------------------------------------------------>|  POST /telegram/link
  |                        |                      |<--------------------|
  |  Account linked!       |                      |  Link account       |
  |<------------------------------------------------|-------------------->|
```

---

### Опция 3: Интегрировать отправку в основное приложение

Я создал файл `telegram-bot.service.ts` который можно использовать для отправки сообщений.

**Ограничение Telegram API:**
- Нельзя отправлять сообщения по username
- Можно отправлять только по `chat_id`
- `chat_id` можно получить только после того, как пользователь **первым** напишет боту

**Поэтому нужен такой flow:**

1. Пользователь регистрируется
2. Фронтенд показывает: "Откройте бота @yourbot и нажмите /start"
3. Бот получает /start и сохраняет `chat_id` пользователя
4. Теперь можно отправлять сообщения через `sendVerificationCodeByChatId(chatId, code)`

---

## 🧪 Проверка настройки

### Проверка Email

```bash
# Запустите приложение
npm run start:dev

# Зарегистрируйте пользователя
curl -X POST http://localhost:3000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "your-real-email@gmail.com",
    "phone": "+79123456789",
    "password": "Test1234",
    "verificationMethod": "email"
  }'

# Проверьте почту!
```

**Если письмо не пришло:**
1. Проверьте логи приложения (должно быть "Email sent")
2. Проверьте папку "Спам"
3. Проверьте настройки SMTP в .env
4. Проверьте что SMTP_PASS - это App Password (для Gmail)

### Проверка Telegram

```bash
# Запустите бота (если создали)
node verification-bot.js

# В другом терминале - зарегистрируйте пользователя
curl -X POST http://localhost:3000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "phone": "+79999999999",
    "password": "Test1234",
    "verificationMethod": "telegram",
    "telegramUsername": "@yourusername"
  }'

# Откройте Telegram, найдите своего бота
# Отправьте команду: /verify
# Бот должен показать код
```

---

## 💡 Рекомендации для продакшена

### Email:
1. **Для MVP/тестирования:** Gmail с App Password
2. **Для продакшена:** Mailgun, SendGrid или свой SMTP сервер
3. **Всегда настраивайте:**
   - SPF запись в DNS
   - DKIM подпись
   - DMARC политику
4. **Используйте поддомен:** mg.yourdomain.com вместо основного домена

### Telegram:
1. **Для MVP:** Используйте простого бота с long polling
2. **Для продакшена:** Настройте webhooks для масштабируемости
3. **Храните chat_id** пользователей после первого /start
4. **Используйте очередь сообщений** (Bull, BullMQ) для надежности

---

## 🔐 Безопасность

### Email:
- ✅ Никогда не коммитьте SMTP пароли в git
- ✅ Используйте App Passwords, не обычные пароли
- ✅ Ротируйте пароли регулярно
- ✅ Мониторьте deliverability rate

### Telegram:
- ✅ Никогда не коммитьте bot token в git
- ✅ Используйте переменные окружения
- ✅ Ограничьте доступ к bot token
- ✅ Используйте webhook secret для валидации запросов

---

## 🆘 Troubleshooting

### Email не отправляется

**Gmail:**
```
Error: Invalid login: 535-5.7.8 Username and Password not accepted
```
**Решение:** Используйте App Password, не обычный пароль

**Yandex:**
```
Error: Connection timeout
```
**Решение:** Проверьте что используете порт 465 и secure: true

### Telegram бот не отвечает

```
Error: 401 Unauthorized
```
**Решение:** Проверьте что TELEGRAM_BOT_TOKEN правильный

```
Error: 403 Forbidden
```
**Решение:** Пользователь заблокировал бота или не запускал /start

---

## 📚 Дополнительные ресурсы

- [Nodemailer документация](https://nodemailer.com/)
- [Gmail App Passwords](https://support.google.com/accounts/answer/185833)
- [Mailgun документация](https://documentation.mailgun.com/)
- [Telegraf документация](https://telegraf.js.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
